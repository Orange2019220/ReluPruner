# Tiny-ImageNet 只启用支持 64x64 输入的 ResNet。
BASE_CONFIG=configs/tinyimagenet_resnet18.env
MODELS="resnet18 resnet34"
BUDGET_TYPE=target_relus
# 下面是用于统一入口的示例绝对预算，请按实验表需求修改。
BUDGETS="196608 98304 49152"
STAGE=all
MODEL_BATCH_SIZES="resnet18:32 resnet34:16"
WORKERS_OVERRIDE=4
